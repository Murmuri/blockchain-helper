import IList from '../../interfaces/list';

const networks: IList[] = [
  { value: 'livenet', name: 'livenet' },
  { value: 'mainnet', name: 'mainnet' },
  { value: 'testnet', name: 'testnet' },
];

export default networks;
